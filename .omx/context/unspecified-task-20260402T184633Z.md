# Context Snapshot

- Task statement: User invoked `$deep-interview` without a concrete task description yet.
- Desired outcome: Clarify the intended change/request enough to produce an execution-ready spec.
- Stated solution: Run the deep-interview workflow.
- Probable intent hypothesis: The user wants a high-confidence clarification pass before planning or implementation.
- Known facts/evidence:
  - Repository is an existing brownfield monorepo named `metrix-parser`.
  - Top-level areas include `apps/web`, `apps/api`, `apps/worker`, `packages/*`, `supabase/`, and docs.
  - `omx explore` is unavailable in this environment because the explore harness depends on Rust/cargo not currently installed.
- Constraints:
  - Deep-interview mode only; no direct implementation.
  - Ask one question per round.
  - Need explicit non-goals and decision boundaries before crystallization.
- Unknowns/open questions:
  - What exact problem or opportunity should be clarified?
  - Why it matters now.
  - Desired outcome, scope, constraints, non-goals, decision boundaries, success criteria.
- Decision-boundary unknowns:
  - What OMX may decide without confirmation.
- Likely codebase touchpoints:
  - Unknown until the user states the target area; likely one or more of `apps/*`, `packages/*`, or `supabase/`.
